server:
  port: 8910

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/breezy?serverTimezone=UTC&rewriteBatchedStatements=true&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: asdfasdf
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      auto-commit: false
      pool-name: Hikari
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
