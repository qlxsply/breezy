package com.corwin.system.file.published;

/**
 * Defines the type of owner for a file or resource.
 *
 * <p>An owner can be a {@code USER} (individual user) or an {@code APPLICATION} (system
 * application).
 *
 * @author Corwin 2026/4/16
 */
public enum OwnerType {
  USER,
  APPLICATION
}
